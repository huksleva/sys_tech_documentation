%!TEX TS-program = lualatex
\documentclass[aspectratio=169]{beamer}

% Настройка языка и шрифтов для LuaLaTeX
\usepackage{fontspec}
\usepackage{polyglossia}
\setmainlanguage{russian}
\setotherlanguage{english}

\setmainfont{DejaVu Sans}
\setsansfont{DejaVu Sans}
\setmonofont{DejaVu Sans Mono}

% Семейства шрифтов для polyglossia
\newfontfamily\cyrillicfont{DejaVu Sans}
\newfontfamily\cyrillicfontsf{DejaVu Sans}
\newfontfamily\cyrillicfonttt{DejaVu Sans Mono}

% Оформление
\usetheme{Madrid}
\usecolortheme{whale}

% Дополнительные пакеты
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{listings}
\usepackage{hyperref}

\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    showstringspaces=false
}

% Метаданные
\title[Инструменты визуализации]{Разработка научных презентаций и постеров в LaTeX}
\author[Тоц Л. А.]{Тоц Леонид Александрович}
\institute[ИВТ-2]{Группа ИВТ-2}
\date{\today}

\begin{document}

% Титульный слайд
\begin{frame}[plain]
    \titlepage
\end{frame}

% Слайд 1: Списки и наложения (overlays)
\begin{frame}{Основы Beamer: Списки и наложения}
    \begin{itemize}
        \item<1-> Презентации компилируются в стандартный формат PDF.
        \item<2-> Механизм наложений (\texttt{overlays}) позволяет пошагово выводить тезисы.
        \item<3-> Поддержка динамического акцентирования внимания слушателей.
    \end{itemize}
\end{frame}

% Слайд 2: Мультиколонки и графика (Векторный график TikZ вместо серой заглушки)
\begin{frame}{Структурирование: Колонки и графика}
    \begin{columns}[c]
        \begin{column}{0.48\textwidth}
            \textbf{Преимущества разделения на колонки:}
            \begin{itemize}
                \item Рациональное использование площади экрана 16:9;
                \item Сопоставление кода и графиков;
                \item Улучшенная читаемость данных.
            \end{itemize}
        \end{column}
        \begin{column}{0.48\textwidth}
            \begin{center}
                \begin{tikzpicture}[scale=0.85]
                    % Координатные оси
                    \draw[->, thick] (0,0) -- (4.5,0) node[right] {\footnotesize $x$};
                    \draw[->, thick] (0,0) -- (0,3.2) node[above] {\footnotesize $f(x)$};
                    \draw[gray!30, dashed] (0,0) grid (4,3);
                    % Графики
                    \draw[thick, blue, smooth] plot coordinates {(0.2,0.3) (1,1.5) (2,2.3) (3,2.7) (4,2.9)};
                    \draw[thick, red, dashed, smooth] plot coordinates {(0.2,0.1) (1,0.6) (2,1.3) (3,2.1) (4,2.8)};
                    % Легенда
                    \node[draw, fill=white, rounded corners=2pt, font=\scriptsize] at (2.6, 0.7) {
                        \textcolor{blue}{--- Модель} \quad \textcolor{red}{- - Данные}
                    };
                \end{tikzpicture}
            \end{center}
        \end{column}
    \end{columns}
\end{frame}

% Слайд 3: Блоки, листинги кода и гиперссылки
\begin{frame}[fragile]{Информационные блоки и листинги}
    \begin{block}{Ключевой вывод}
        Окружение \texttt{lstlisting} в сочетании с опцией \texttt{[fragile]} позволяет оформлять исходный код без синтаксических сбоев.
    \end{block}

    \vspace{0.5em}
    \begin{lstlisting}[language=Python]
def calculate_metrics(data):
    # Обработка выборки
    return sum(data) / len(data)
    \end{lstlisting}

    \vspace{0.5em}
    Документация доступна по ссылке: \href{https://ctan.org/pkg/beamer}{\color{blue}\underline{CTAN: Beamer Package}}.
\end{frame}

\end{document}
